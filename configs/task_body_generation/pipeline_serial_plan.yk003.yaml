serial_plan:
  # 串行版 v1：所有 stage / task 按顺序执行
  continue_on_error: false
  stages:
    # - name: dataloader
    #   tasks:
    #     - name: dataloader_norm
    #       config: configs/task_body_generation/dataloader_norm/body_test.yaml

    - name: generation
      tasks:
        # - name: prompt_only
        #   config: configs/task_body_generation/generation/body_pose_prompt.yaml
        - name: prompt_canny
          config: configs/task_body_generation/generation/body_pose_prompt_canny.yaml

    # - name: filter
    #   tasks:
    #     - name: phase1
    #       config: test/test-filters/configs/filter_compose.yaml
    #     - name: phase2
    #       config: test/test-filters/configs/filter_compose.yaml
    #     - name: phase3
    #       config: test/test-filters/configs/filter_compose.yaml
