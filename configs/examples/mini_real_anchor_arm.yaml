run:
  run_id: mini_real_anchor_arm
  artifacts_root: artifacts/runs

dataloader:
  real_dir: artifacts/datasets/real_data_arm
  label_dir: artifacts/datasets/real_dataset_labels
  classes_path: artifacts/datasets/real_dataset_labels/classes.txt
  patterns:
    - "*.png"
  require_labels: true
  max_samples: 0
  naming:
    canonicalize_names: true
    task_name: arm_injection
    id_width: 5
    id_start: 1
    materialize_mode: copy
