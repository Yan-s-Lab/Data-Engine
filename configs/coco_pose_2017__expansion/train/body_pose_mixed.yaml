run:
  run_id: body_pose_coco_train_mixed
  artifacts_root: artifacts/runs/yk003/body_pose_coco

train_yolo:
  # Prerequisite: both real_dataset and ai_dataset must exist
  dataset_yaml: artifacts/runs/yk003/body_pose_coco/body_pose_coco_mixed/label/mixed_dataset/dataset.yaml
  model: yolo11s-pose.pt
  epochs: 100
  imgsz: 640
  batch: 16
  device: "0"
  workers: 4
  patience: 30
