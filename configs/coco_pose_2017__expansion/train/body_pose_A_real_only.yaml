run:
  run_id: body_pose_coco_A_real_only
  artifacts_root: artifacts/runs/yk003/body_pose_coco

train_yolo:
  dataset_yaml: artifacts/runs/yk003/body_pose_coco/body_pose_coco_real/label/real_train_anchor/dataset.yaml
  model: yolo11s-pose.pt
  epochs: 100
  imgsz: 640
  batch: 16
  device: "0"
  workers: 4
  patience: 30
