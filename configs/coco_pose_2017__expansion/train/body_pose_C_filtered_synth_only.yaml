run:
  run_id: body_pose_coco_C_filtered_synth_only
  artifacts_root: artifacts/runs/yk003/body_pose_coco

train_yolo:
  dataset_yaml: artifacts/runs/yk003/body_pose_coco/body_pose_coco_annotation/label/ai_dataset/dataset.yaml
  model: yolo11s-pose.pt
  epochs: 100
  imgsz: 640
  batch: 16
  device: "0"
  workers: 4
  patience: 30

pipeline:
  steps: [train_yolo_pose]
  resume_from_artifacts: true
